;;
;; アカウント情報管理を行うアクションハンドラ
;;
(ns wiki.plugin.admin.account-handler
  (:use
   clojure.tools.logging)
  (:require [wiki.default-storage :as db]
            [wiki.wiki :as wiki]))

(defn do-action [req]
  "Hello")
